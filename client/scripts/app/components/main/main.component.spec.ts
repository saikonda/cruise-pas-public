import { MainComponent } from './main.component';

describe('Component: MainComponent', () => {
    // Main Component is too large to test
    it("Should work", () => {
        expect('hello').toBe('hello')
    })
});